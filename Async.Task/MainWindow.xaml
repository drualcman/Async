<Window x:Class="Async.Tasks.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Async.Tasks"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" x:Name="Messages" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                    HorizontalAlignment="Center" >
            <Button x:Name="StartTask" Content="Iniciar tarea" 
                    Margin="5" Padding="5"
                    Click="StartTask_Click"/>
            <Button x:Name="CancelTask" Content="Cancelar tarea" 
                    Margin="5" Padding="5"
                    Click="CancelTask_Click"/>
            <Button x:Name="ShowStatus" Content="Mostrar estado tarea" 
                    Margin="5" Padding="5"
                    Click="ShowStatus_Click"/>
        </StackPanel>
    </Grid>

</Window>
